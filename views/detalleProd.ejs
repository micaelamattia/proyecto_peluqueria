<!DOCTYPE html>
<html lang="es">
<%- include('partials/head') %> 
<body>
   <%-include('partials/header') %> 
 
    <div class="container">
        <section>
            <div class="row">
               <div class="col-12">
                   <h2>Detalle del producto: <%= producto.name %> </h2>
               </div>
               <div class="col-12">
                   <div class="contenido">
                       <div class="row">
                           <div class="col-12">
                               <figure class="text-center">
                                   <img src="../images/<%= producto.image %> " class="img-fluid" alt="">
                               </figure>
                               <p>
                                   <%= producto.description %> 
                               </p>
                           </div>

                           <div class="col-12">
                               <h4>
                                   <%= producto.name %> 
                               </h4>
                             <% if (producto.discount!=0) { %>
                                <span> <strike> $<%= producto.price.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ".") %></strike> / <strong><%= producto.discount %> % OFF</strong>
                                </span>
                             <% } %>
                               <h4>$
                                <%- (producto.price -producto.price * producto.discount /100).toFixed(0).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ".") %>
                               </h4>
                               <p><i class="fas fa-credit-card mr-3 my-4"></i>Pagá en 12 cuotas sin interés</p>
                               <p><i class="fas fa-store mr-3 my-4"></i>Retiro gratis en locales del vendedor</p>
                               <button>Comprar ahora</button>

                            </div>

                        </div>
                    </div>
                </div>
            
            </div>
        </section>
    </div>



    <%- include('partials/footer') %> 
</body>
</html>